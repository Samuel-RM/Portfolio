---
const { class: className, id } = Astro.props;

// diferentes maneras de permitir mas clases a los componentes una vez implementados
---

<section id={id} class={`w-full lg:w-[740px] mx-auto  ${className}`}>
  <!-- <section class={`w-full lg:w-[740px] mx-auto  ${Astro.props.class}`}> -->
  <slot />
</section>
